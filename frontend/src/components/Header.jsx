/**
 * Header.jsx — NOT USED
 *
 * This component is never mounted. AppShell renders its own <header> directly
 * and owns the /health poll so model status is tied to the shell's lifetime.
 *
 * If you want a standalone header in the future, move the health-fetch logic
 * from AppShell into this file and import it there instead of duplicating it.
 *
 * Safe to delete this file.
 */